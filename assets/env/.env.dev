SUPABASE_URL="https://uwivfcxywrzcnyouuydj.supabase.co"
SUPABASE_ANON_KEY="sb_publishable_lgw4BdzfxFNLzS7bpEn5dw_f1q84iil"

# Supabase Storage bucket for PDF + signature cloud backup
SUPABASE_STORAGE_BUCKET="voltcore-files"

# Active tenant UUID stamped on every synced row (required for cloud sync).
# This is a public client bundle: never add service-role or secret keys here.
SUPABASE_TENANT_ID="94f5d3dc-bbdf-42a3-a70e-1d19029f3ee3"

APP_NAME="Voltcore Dev"
API_BASE_URL="http://localhost:3000"
PDF_OUTPUT_PATH="AandSElectric/Dev"

# Email: sending is now server-side via the `send-report-email` Supabase Edge
# Function. SMTP host/user/pass live as Edge Function secrets (supabase secrets
# set ...), NOT in this bundle. Only SMTP_TO (default recipient) is read by the
# client and is not a secret.
SMTP_TO="office@aselectricnyc.com"

# Optional secure overrides
USE_SECURE_ENV=false
SECURE_SUPABASE_URL=""
SECURE_SUPABASE_ANON_KEY=""

# Observability
SENTRY_DSN="https://xxxx@sentry.io/12345"
CRASHLYTICS_API_KEY="your-crashlytics-key"

# App metadata (nice-to-have, especially for CI)
APP_ICON_NAME="ic_launcher"
BUNDLE_ID="com.theoreticalminds.voltcore.dev"
