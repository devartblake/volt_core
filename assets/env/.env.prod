SUPABASE_URL="https://uwivfcxywrzcnyouuydj.supabase.co"
SUPABASE_ANON_KEY="sb_publishable_lgw4BdzfxFNLzS7bpEn5dw_f1q84iil"

# Supabase Storage bucket for PDF + signature cloud backup
SUPABASE_STORAGE_BUCKET="voltcore-files"

# Active tenant UUID stamped on every synced row (required for cloud sync).
SUPABASE_TENANT_ID="94f5d3dc-bbdf-42a3-a70e-1d19029f3ee3"

APP_NAME="Voltcore"
API_BASE_URL="https://api.voltcore.com"
PDF_OUTPUT_PATH="AandSElectric"

# Email: sending is server-side via the `send-report-email` Edge Function.
# SMTP secrets live as Edge Function secrets (supabase secrets set ...), never
# in this client bundle. Only SMTP_TO (default recipient) is read by the client.
SMTP_TO="office@aselectricnyc.com"
